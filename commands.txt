git init

git remote add origin "?https://github.com/ybnagendra/git-myRepository01.git"

 git remote remove origin

git pull origin master

git status

 git commit -m "adding first commit in local repo"
 {
if not working execute
git config --global user.email "miservices.mys@gmail.com"

 git config --global user.name "ybnagendra"
then again 

git commit -m "adding first commit in local repo"
}